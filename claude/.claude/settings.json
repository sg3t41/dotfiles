{
	"mcpServers": {
		"coincheck": {
			"command": "coincheck-mcp",
			"env": {
				"COINCHECK_API_KEY": "${COINCHECK_API_KEY}",
				"COINCHECK_API_SECRET": "${COINCHECK_API_SECRET}"
			},
			"args": [
				"${COINCHECK_MCP_SERVER_PATH}"
			]
		}
	},
	"selectedAuthType": "gemini-api-key",
	"theme": "Default"
}
